# SRI OS Command Center — frontend environment
# Copy to .env.local and adjust as needed

# URL of the FastAPI backend (no trailing slash)
# Default: http://localhost:8000
VITE_API_URL=http://localhost:8000
